// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost'], // Thêm domain của server ảnh
    },
  };
  
export default nextConfig;
  